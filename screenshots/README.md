# Screenshots

Add screenshots here that show the running system, for example:

1. `01-eks-cluster.png` - `eksctl get cluster` / AWS Console showing the cluster is `ACTIVE`.
2. `02-nodes.png` - `kubectl get nodes -o wide`.
3. `03-pods.png` - `kubectl get pods -n namegen -o wide` (app + mongodb Running).
4. `04-pvc.png` - `kubectl get pvc -n namegen` and the bound PersistentVolume.
5. `05-service-nlb.png` - `kubectl get svc -n namegen` showing the NLB `EXTERNAL-IP`/hostname.
6. `06-app-webpage.png` - the app in a browser, showing a generated name and the stored names list.
7. `07-github-actions-run.png` - a successful GitHub Actions workflow run (build + deploy).
8. `08-ecr-repo.png` - the ECR repository with the pushed image tag(s).
